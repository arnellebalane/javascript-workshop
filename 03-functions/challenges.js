/**
 *  Tilde Community - Javascript Workshop
 *  Challenge Set 3
 *
 *  Fill up the blank spaces with your code solutions. When you're done,
 *  open the `test-runner.html` file in your browser.
 *
 *  Challenges in this set are derived from the problems in Douglas Crockford's
 *  Javascript The Good Parts.
 *
 *  Terms:
 *      - unary function: a function that accepts one parameter
 *      - binary function: a function that accepts two parameters
 **/


// 01. Write a unary function named `identity` which accepts a parameter and
// returns that parameter.
// test case: identity(3) => 3





// 02. Write a binary function named `multiply` which accepts two parameters
// and returns their product.
// test case: multiply(2, 3) => 6





// 03. Write a unary function named `identityf` which accepts a parameter and
// returns a function that returns that parameter.
// test case: identityf(3)() => 3





// 04. Write a unary function named `multiplyf` which accepts one parameter,
// returns another unary function that accepts another parameter and returns
// the product of the two parameters.
// test case: multiplyf(2)(3) => 6





// 05. Write a function named `twice` which accepts a binary function and
// returns a unary function which passes the parameter it receives to the
// binary function twice.
// test case: twice(multiply)(3) => 9





// 06. Define the function named `square` using the functions that you've
// written in the previous challenges.





// 07. Write a function named 'compose' which accepts two unary functions as
// parameters and composes those functions.
// test case: compose(square, identity)(3) => square(identity(3)) => 9
