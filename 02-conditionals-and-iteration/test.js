const assert = chai.assert;

describe('Conditionals and Iteration', () => {
  it('Using a loop, get the sum of the first 10 positive integers. Store this result in a variable named "answer1".', () => {
    assert.equal(answer1, 55);
  });

  it('Using a loop, get the sum of the first 10 positive integers that are divisible by 3 or 5. Store this result in a variable named "answer2".', () => {
    assert.equal(answer2, 119);
  });

  it('Using a loop, get the product of the first 13 positive integers. Store whether this number is an even number in a variable named "answer3".', () => {
    assert.isTrue(answer3);
  });
});
